# Migration `20200628191304-add-uuid-as-birthday-id`

This migration has been generated by Alexandre Santos at 6/28/2020, 7:13:04 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "its-your-birthday"."Contributor" (
"birthdayId" text   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text  NOT NULL ,"hasPaid" boolean  NOT NULL DEFAULT false,"id" SERIAL,"name" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "its-your-birthday"."Gift" (
"authorId" integer   ,"birthdayId" text   ,"description" text  NOT NULL ,"id" SERIAL,"url" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "its-your-birthday"."Birthday" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"date" timestamp(3)   ,"id" text  NOT NULL ,"person" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "its-your-birthday"."_upvotedBy" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE UNIQUE INDEX "_upvotedBy_AB_unique" ON "its-your-birthday"."_upvotedBy"("A","B")

CREATE  INDEX "_upvotedBy_B_index" ON "its-your-birthday"."_upvotedBy"("B")

ALTER TABLE "its-your-birthday"."Contributor" ADD FOREIGN KEY ("birthdayId")REFERENCES "its-your-birthday"."Birthday"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "its-your-birthday"."Gift" ADD FOREIGN KEY ("birthdayId")REFERENCES "its-your-birthday"."Birthday"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "its-your-birthday"."Gift" ADD FOREIGN KEY ("authorId")REFERENCES "its-your-birthday"."Contributor"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "its-your-birthday"."_upvotedBy" ADD FOREIGN KEY ("A")REFERENCES "its-your-birthday"."Contributor"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "its-your-birthday"."_upvotedBy" ADD FOREIGN KEY ("B")REFERENCES "its-your-birthday"."Gift"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200628191304-add-uuid-as-birthday-id
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,44 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+  platform = ["debian-openssl-1.1.x", "native"]
+}
+
+model Contributor {
+  id            Int       @default(autoincrement()) @id
+  email         String
+  name          String
+  hasPaid       Boolean   @default(false)
+  Birthday      Birthday? @relation(fields: [birthdayId], references: [id])
+  birthdayId    String?
+  suggestedGifs Gift[]    @relation("giftSuggestor")
+  upvotes       Gift[]    @relation("upvotedBy")
+  createdAt     DateTime  @default(now())
+}
+
+model Gift {
+  id          Int           @default(autoincrement()) @id
+  Birthday    Birthday?     @relation(fields: [birthdayId], references: [id])
+  birthdayId  String?
+  url         String
+  description String
+  suggestor   Contributor?  @relation(name: "giftSuggestor", fields: [authorId], references: [id])
+  authorId    Int?
+  upvotedBy   Contributor[] @relation("upvotedBy")
+}
+
+model Birthday {
+  id           String        @default(uuid()) @id
+  createdAt    DateTime      @default(now())
+  date         DateTime?
+  person       String
+  gifts        Gift[]
+  contributors Contributor[]
+}
```


