# Migration `20210206231708`

This migration has been generated by Alexandre Santos at 2/6/2021, 11:17:08 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Contributor" (
"birthdayId" text   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text  NOT NULL ,"hasPaid" boolean  NOT NULL DEFAULT false,"name" text  NOT NULL ,
    PRIMARY KEY ("email"))

CREATE TABLE "public"."Gift" (
"authorEmail" text   ,"birthdayId" text   ,"description" text  NOT NULL ,"id" SERIAL,"url" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Birthday" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"date" timestamp(3)   ,"id" text  NOT NULL ,"person" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."_upvotedBy" (
"A" text  NOT NULL ,"B" integer  NOT NULL )

CREATE UNIQUE INDEX "_upvotedBy_AB_unique" ON "public"."_upvotedBy"("A","B")

CREATE  INDEX "_upvotedBy_B_index" ON "public"."_upvotedBy"("B")

ALTER TABLE "public"."Contributor" ADD FOREIGN KEY ("birthdayId")REFERENCES "public"."Birthday"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Gift" ADD FOREIGN KEY ("birthdayId")REFERENCES "public"."Birthday"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Gift" ADD FOREIGN KEY ("authorEmail")REFERENCES "public"."Contributor"("email") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."_upvotedBy" ADD FOREIGN KEY ("A")REFERENCES "public"."Contributor"("email") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_upvotedBy" ADD FOREIGN KEY ("B")REFERENCES "public"."Gift"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20210206231708
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,43 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+  platform = ["debian-openssl-1.1.x", "native"]
+}
+
+model Contributor {
+  email         String    @id
+  name          String
+  hasPaid       Boolean   @default(false)
+  Birthday      Birthday? @relation(fields: [birthdayId], references: [id])
+  birthdayId    String?
+  suggestedGifs Gift[]    @relation("giftSuggestor")
+  upvotes       Gift[]    @relation("upvotedBy")
+  createdAt     DateTime  @default(now())
+}
+
+model Gift {
+  id          Int           @id @default(autoincrement())
+  Birthday    Birthday?     @relation(fields: [birthdayId], references: [id])
+  birthdayId  String?
+  url         String
+  description String
+  suggestor   Contributor?  @relation(name: "giftSuggestor", fields: [authorEmail], references: [email])
+  authorEmail String?
+  upvotedBy   Contributor[] @relation("upvotedBy")
+}
+
+model Birthday {
+  id           String        @id @default(uuid())
+  createdAt    DateTime      @default(now())
+  date         DateTime?
+  person       String
+  gifts        Gift[]
+  contributors Contributor[]
+}
```


