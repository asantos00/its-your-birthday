# Migration `20200627025135-add-upvotes-and-gift-suggestor`

This migration has been generated by Alexandre Santos at 6/27/2020, 2:51:35 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "its-your-brirthday"."_upvotedBy" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

ALTER TABLE "its-your-brirthday"."Gift" DROP CONSTRAINT IF EXiSTS "Gift_contributorId_fkey",
DROP COLUMN "contributorId",
ADD COLUMN "authorId" integer   ;

CREATE UNIQUE INDEX "_upvotedBy_AB_unique" ON "its-your-brirthday"."_upvotedBy"("A","B")

CREATE  INDEX "_upvotedBy_B_index" ON "its-your-brirthday"."_upvotedBy"("B")

ALTER TABLE "its-your-brirthday"."_upvotedBy" ADD FOREIGN KEY ("A")REFERENCES "its-your-brirthday"."Contributor"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "its-your-brirthday"."_upvotedBy" ADD FOREIGN KEY ("B")REFERENCES "its-your-brirthday"."Gift"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "its-your-brirthday"."Gift" ADD FOREIGN KEY ("authorId")REFERENCES "its-your-brirthday"."Contributor"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200627002356-birthday-date-optional..20200627025135-add-upvotes-and-gift-suggestor
--- datamodel.dml
+++ datamodel.dml
@@ -2,33 +2,35 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
 model Contributor {
-  id         Int       @default(autoincrement()) @id
-  email      String
-  name       String
-  upvotes    Gift[]
-  hasPaid    Boolean   @default(false)
-  Birthday   Birthday? @relation(fields: [birthdayId], references: [id])
-  birthdayId Int?
+  id            Int       @default(autoincrement()) @id
+  email         String
+  name          String
+  hasPaid       Boolean   @default(false)
+  Birthday      Birthday? @relation(fields: [birthdayId], references: [id])
+  birthdayId    Int?
+  suggestedGifs Gift[]    @relation("giftSuggestor")
+  upvotes       Gift[]    @relation("upvotedBy")
 }
 model Gift {
-  id            Int          @default(autoincrement()) @id
-  Birthday      Birthday?    @relation(fields: [birthdayId], references: [id])
-  birthdayId    Int?
-  url           String
-  description   String
-  Contributor   Contributor? @relation(fields: [contributorId], references: [id])
-  contributorId Int?
+  id          Int           @default(autoincrement()) @id
+  Birthday    Birthday?     @relation(fields: [birthdayId], references: [id])
+  birthdayId  Int?
+  url         String
+  description String
+  suggestor   Contributor?  @relation(name: "giftSuggestor", fields: [authorId], references: [id])
+  authorId    Int?
+  upvotedBy   Contributor[] @relation("upvotedBy")
 }
 model Birthday {
   id           Int           @default(autoincrement()) @id
```


