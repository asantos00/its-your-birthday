# Migration `20200626234321-init`

This migration has been generated by Alexandre Santos at 6/26/2020, 11:43:21 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "its-your-brirthday"."Contributor" (
"birthdayId" integer   ,"email" text  NOT NULL ,"hasPaid" boolean  NOT NULL DEFAULT false,"id" SERIAL,"name" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "its-your-brirthday"."Gift" (
"birthdayId" integer   ,"contributorId" integer   ,"description" text  NOT NULL ,"id" SERIAL,"url" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "its-your-brirthday"."Birthday" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"date" timestamp(3)  NOT NULL ,"id" SERIAL,"person" text  NOT NULL ,
    PRIMARY KEY ("id"))

ALTER TABLE "its-your-brirthday"."Contributor" ADD FOREIGN KEY ("birthdayId")REFERENCES "its-your-brirthday"."Birthday"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "its-your-brirthday"."Gift" ADD FOREIGN KEY ("birthdayId")REFERENCES "its-your-brirthday"."Birthday"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "its-your-brirthday"."Gift" ADD FOREIGN KEY ("contributorId")REFERENCES "its-your-brirthday"."Contributor"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200626234321-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,40 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Contributor {
+  id         Int       @default(autoincrement()) @id
+  email      String
+  name       String
+  upvotes    Gift[]
+  hasPaid    Boolean   @default(false)
+  Birthday   Birthday? @relation(fields: [birthdayId], references: [id])
+  birthdayId Int?
+}
+
+model Gift {
+  id            Int          @default(autoincrement()) @id
+  Birthday      Birthday?    @relation(fields: [birthdayId], references: [id])
+  birthdayId    Int?
+  url           String
+  description   String
+  Contributor   Contributor? @relation(fields: [contributorId], references: [id])
+  contributorId Int?
+}
+
+model Birthday {
+  id           Int           @default(autoincrement()) @id
+  createdAt    DateTime      @default(now())
+  date         DateTime
+  person       String
+  gifts        Gift[]
+  contributors Contributor[]
+}
```


